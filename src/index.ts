export { createSupabaseAuth } from './client';
export { AuthError } from './types';
export type { SupabaseAuthConfig, SupabaseUser } from './types';
export { extractBearerToken } from './middleware/generic';
